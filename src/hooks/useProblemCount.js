import { useState } from 'preact/hooks';

// Stub hook for problem count (replace with real logic later)
export function useProblemCount() {
  // Hardcoded value for now
  const [count] = useState(0);
  return count;
}
